apiVersion: v1
kind: Service
metadata:
  labels:
    service: backend-user-svc
  name: backend-user-svc
spec:
  selector:
    service: backend-user-svc
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080