apiVersion: v1
kind: Service
metadata:
  labels:
    service: backend-feed-svc
  name: backend-feed-svc
spec:
  selector:
    service: backend-feed
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080